# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

/node_modules
/frontend/node_modules
/backend/node_modules

# Secret environment files — NEVER commit these
.env
.env.*
!.env.example
!.env.production
backend/.env
backend/.env.*
!backend/.env.example
frontend/.env.local

# NOTE: .env.production IS committed intentionally.
# It only contains VITE_API_URL (a public URL, not a secret).
# This is needed so Vercel can pick it up during the build.
# Real secrets (MONGO_URI, JWT_SECRET) must be set in Vercel dashboard.

.DS_Store
dist
build

# Winston structured log files — contain stack traces and potentially sensitive data
backend/combined.log
backend/error.log
backend/error_log.txt
backend/test_out.txt
backend/test_output.txt
*.log

# Scratch/temp files
backend/scratch_lean.js
backend/update_logger.py

# Test coverage
coverage/
*.lcov
.nyc_output

# Docker override files with secrets
docker-compose.override.yml

# Playwright artifacts
test-results/
playwright-report/